export type IncomeConfigInput = {
  userId: string;
  income_config_day: number;
  memo: string;
  amount: number;
  sort: number;
};

export type IncomeConfigOutput = {
  userId: string;
  income_config_day: number;
  memo: string;
  amount: number;
  sort: number;
};
